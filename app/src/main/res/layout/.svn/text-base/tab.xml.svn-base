<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal" >
  <TextView
        android:layout_width="fill_parent"
        android:layout_height="1dip"
        android:layout_below="@id/lin_column"
        android:background="@color/title_bg" >
    </TextView>
    <RadioGroup
        android:id="@+id/radgp_menu"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginTop="1dip"
        android:background="#ffffff"
        android:orientation="horizontal" >

        <RadioButton
            android:id="@+id/tab_item_0"
            style="@style/radioButtonStyle"
            android:layout_width="0dip"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:drawableTop="@drawable/tab_4"
            android:paddingTop="2.0dip"
            android:text="图书榜"
            android:textColor="@drawable/tab_textcolor"
            android:textSize="12sp" />

        <!-- <RadioButton -->
        <!-- android:id="@+id/tab_item_1" -->
        <!-- style="@style/radioButtonStyle" -->
        <!-- android:layout_width="0dip" -->
        <!-- android:layout_height="wrap_content" -->
        <!-- android:layout_weight="2" -->
        <!-- android:drawableTop="@drawable/icon3" -->
        <!-- android:paddingTop="2.0dip" -->
        <!-- android:text="论坛" -->
        <!-- android:textSize="12sp" /> -->

        <RadioButton
            android:id="@+id/tab_item_2"
            style="@style/radioButtonStyle"
            android:layout_width="0dip"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:drawableTop="@drawable/tab_2"
            android:paddingTop="2.0dip"
            android:text="我的豆瓣"
            android:textColor="@drawable/tab_textcolor"
            android:textSize="12sp" />

        <RadioButton
            android:id="@+id/tab_item_3"
            style="@style/radioButtonStyle"
            android:layout_width="0dip"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:drawableTop="@drawable/tab_1"
            android:paddingTop="2.0dip"
            android:text="个人中心"
            android:textColor="@drawable/tab_textcolor"
            android:textSize="12sp" />
    </RadioGroup>

    <!-- <TextView -->
    <!-- android:layout_width="fill_parent" -->
    <!-- android:layout_height="1dp" -->
    <!-- android:layout_alignTop="@id/radgp_menu" -->
    <!-- android:background="#C3B59A" /> -->

</RelativeLayout>